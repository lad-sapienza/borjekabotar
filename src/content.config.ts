/**
 * Content Collections Configuration
 *
 * This site keeps its page bodies as flat MDX in `src/content-pages/` (imported
 * directly by the matching `src/pages/*.astro` wrapper), so there are no
 * `astro:content` collections to define.
 */
export const collections = {};
